# -*- coding: utf-8 -*-

from enum import Enum, auto


class DialogChoice(Enum):
    ABOUT = auto()
    ENHANCE = auto()
    FILECHOOSER = auto()
    KEYBINDINGS = auto()
    PREFERENCES = auto()
    PROPERTIES = auto()
